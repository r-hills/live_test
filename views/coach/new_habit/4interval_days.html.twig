<!-- Header twig template -->

{% set vars = {
  'page_title': 'New Habit',
  'css_url': '/css/new_habit.css'
}

%}
{% include 'partials/header.html.twig' with vars %}

    <body>
      <!-- Navbar twig template -->

      {% set active_page = '/habit/new_habit_motivation' %}
      {% include 'partials/nav.html' %}


    <div class = "container" id = "mainContainer">
      <div class = "row row-centered" id = "mainRow">
        <div class = "col-xl-8 col-lg-8 col-md-11 col-sm-12 col-sm-offset-1 col-centered mainBody" id="habits">

          <h1>Here's what you wrote</h1>
          <p style = "font-style: italic;">"{{ dump }}"</p>

          <p>Based on what you wrote above, how many days do you think you can do this habit in a row?</p>

        </div>

        <div class = "col-xl-5 col-lg-5 col-md-5 col-sm-5 col-sm-offset-1 col-centered mainBody" id="habits">

            <!-- If possible, this should be some kind of input that allows the user to enter a bulleted list -->
          <form action="/coach/new_habit/{{ habit.getId }}/update_intervaldays" method="post">
            <div class = "form-group">
              <input type="integer" name="intervaldays" class = "form-control">
              <!-- pass dump in hidden post so user can continue to reference it-->
              <input type="hidden" name="dump" value="{{dump}}">

            </div>
                <button type="submit" class = "btn btn-primary">Submit</button>
            </form>
        </div>

      </div>
    </div>
  </body>
</html>
